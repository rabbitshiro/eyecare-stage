/**
 * This will be the configurations
 * needed for sites with multiple locales.
 */
export const LOCALES = ["ww", "en"];
export const LOCALE_LABELS = [{ label: "Ww", value: "ww" }, { label: "English", value: "en" }];
export const DEFAULT_LOCALE = LOCALES[0];
